/** German labels for a session's `source`. One place so the timeline and drill-down never diverge
 *  (and 'assigned' has a single spot to land when the §H1 rails arrive). */
export const SOURCE_LABEL: Record<string, string> = {
  bank: 'Übung',
  llm: 'KI-Übung',
  homework: 'Hausübung',
  assigned: 'Zugewiesene Übung',
};
