# besserlesenschreiben reviewer portal — example environment. Copy to `.env`:  cp .env.example .env

# Backend API base URL (includes the /api/v1 prefix). The staff routes live under /api/v1/staff/*.
# Dev default points at the local backend.
VITE_API_BASE=http://localhost:3000/api/v1
