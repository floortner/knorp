# besserlesenschreiben frontend — example environment. Copy to `.env`:  cp .env.example .env

# Backend API base URL (includes the /api/v1 prefix). Dev default points at the local backend.
VITE_API_BASE=http://localhost:3000/api/v1

# Enable the installable PWA layer.
VITE_PWA=true
